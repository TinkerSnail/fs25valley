# Editor / agent settings — shared hooks & process ARE version-controlled (see .claude/),
# but local-only overrides and per-session runtime markers are not.
.claude/settings.local.json
.claude/.session-start
.claude/.preflight
.cursor/

# macOS
.DS_Store

# Build artifacts
*.zip

# Decompiled GIANTS base-game scripts + extracted config (API research dumps).
# Local reference only — these are GIANTS-copyrighted; do NOT push to the public repo (EULA).
journals/dumps/api/
